# install runtime dependencies
-r requirements.txt

# load local integration-test configuration
python-dotenv

# run tests and build release artifacts
pytest
build

# generate docs
sphinx==9.1.0
sphinx_rtd_theme
